📌 Project Description

A simple Java console application that checks the temperature entered by the user.

The program:

Accepts user input using Scanner

Converts the input to UPPERCASE using toUpperCase()

Compares the value using equals()

Displays a message based on the result

🛠 Technologies Used

Java

Scanner Class

String Methods

Conditional Statements (if-else)

▶ How It Works

The user enters a temperature value (hot / cold).

The program converts the input to uppercase.

It compares the value with predefined options.

A message is printed depending on the input.
